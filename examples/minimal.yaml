# Smallest legal team — one lead + one engineer, no QA.
# `gala_team --team examples/minimal.yaml --project .`

teams:
  - key: main
    name: "Lone"
    description: "Solo team — lead + one engineer, no QA"
    members:
      - role: team_lead
        name: "Lead"
        personality: "Decides and reviews."

      - role: engineer
        name: "Eng"
        personality: "Implements."

workflow:
  qa_required: false
